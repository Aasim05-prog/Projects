<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Home Page</title>
</head>

<body>
    <h1>url shortener</h1>

    <% if (id) { %>
        <p>
            URL Generated:
            <a href="http://localhost:9000/<%= id %>" target="_blank">
                http://localhost:9000/<%= id %>
            </a>
        </p>
    <% } %>

    <form action="/url" method="post">
        <input type="text" name="url" placeholder="Enter original url" />
        <button type="submit">Generate</button>
    </form>

    <div>
        <% if (urls && urls.length > 0) { %>
            <table border="2">
                <tr>
                    <th>SR.NO</th>
                    <th>shortid</th>
                    <th>redirect</th>
                    <th>clicks</th>
                </tr>
                <% urls.forEach((url, index) => { %>
                    <tr>
                        <td><%= index + 1 %></td>
                        <td><%= url.shorturl %></td>
                        <td><a href="<%= url.originalUrl %>" target="_blank"><%= url.originalUrl %></a></td>
                        <td><%= url.visithistory.length %></td>
                    </tr>
                <% }) %>
            </table>
        <% } %>
    </div>
</body>

</html>
